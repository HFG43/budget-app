<div class="logo_container">
  <h1 class="logo">Smart Spend</h1>
</div>  

<section class="sign_section">
  <% if user_signed_in? %>
    <script>
      window.location.href = '<%= groups_path %>';
    </script>
    <%# TEMP SOLUTION / THEN WILL USE AGAIN THIS LINK %>
    <%= link_to 'Sign Out', destroy_user_session_path, data: { turbo_method: :delete } %>
    <%# TEMP SOLUTION / THEN WILL USE AGAIN THIS LINK %>
  <% else %>
    <%= link_to 'Log In', new_user_session_path, class: 'log_in'%>
    <%= link_to 'Sign Up', new_user_registration_path, class: 'sign_up' %>
  <% end %>
</section>
